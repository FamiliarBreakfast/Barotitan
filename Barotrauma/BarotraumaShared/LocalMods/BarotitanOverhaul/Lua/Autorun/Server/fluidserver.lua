if SERVER and not Game.IsSingleplayer then
    --hook on fluid changes
--     local message = Networking.Start("fluidSync")
--     message.WriteString("test") --send fluid data
--     for client in Client.ClientList do
--         Networking.Send(message, client)
--     end
end
